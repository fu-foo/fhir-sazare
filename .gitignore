# Build artifacts
/target/
ui/node_modules/
ui/dist/

# SQLite databases
data/
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Config with credentials (use config.example.yaml as template)
config.yaml
config.demo.yaml
config.docker.yaml
config.public.yaml
Dockerfile.fly

# TLS certificates and private keys
certs/*.key
certs/*.crt
certs/*.pem

# Fly.io (local deploy only)
fly.toml
fly.public.toml
.github/workflows/fly-deploy.yml

# Claude Code
.claude/
CLAUD.md

# Local agent navigation aids (small-context model helpers, updated locally — not for the repo)
AGENTS.md
docs/
scripts/

# Aider (local AI pair-programming experiments — config, history, caches)
.aider*

# Personal notes
memo.txt

# OS files
.DS_Store
Thumbs.db
