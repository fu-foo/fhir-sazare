services:
  sazare:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - sazare-data:/data
      - ./config.yaml:/etc/sazare/config.yaml:ro
    environment:
      - SAZARE_DATA_DIR=/data
      - RUST_LOG=info

volumes:
  sazare-data:
